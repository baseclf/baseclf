# The demo deployment the public example page talks to (decision Q8).
# Not a secret: every visitor's browser calls this address directly, and the
# deployment only ever answers what its policies allow. Committed so a
# production build is deterministic instead of depending on a shell variable.
VITE_BASECLF_URL=https://baseclf-demo.raspy-firefly-4c0b.workers.dev
