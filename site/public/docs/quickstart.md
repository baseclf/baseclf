# Quickstart

This preview flow demonstrates the intended setup order. CLI commands and endpoints are mock data.

```sh
npx baseclf init demo-app
cd demo-app
npx baseclf dev
```

Define a table and a policy, then request the generated REST endpoint with a user token. Keep admin tokens out of browser bundles.
